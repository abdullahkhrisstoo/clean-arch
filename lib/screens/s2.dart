import 'package:flutter/material.dart';

class S2 extends StatelessWidget {
  const S2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('hello 2')),
      body: Center(child: Text('body2')),
    );
  }
}
